package codingame.challenge.packman

import codingames.challenge.pacman.Player
import org.scalatest.FlatSpec

class PlayerTest extends FlatSpec {
  "Player" should "run the application" in {
    Player.main(Array.empty[String])
  }
}
